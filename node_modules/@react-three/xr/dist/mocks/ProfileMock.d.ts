import { Profile } from 'three-stdlib';
export declare class ProfileMock implements Profile {
    constructor({ profileId, fallbackProfileIds, layouts }?: {
        profileId?: string;
        fallbackProfileIds?: string[];
        layouts?: Profile['layouts'];
    });
    profileId: string;
    fallbackProfileIds: string[];
    layouts: Profile['layouts'];
}
