import { Group, Texture, Object3D } from 'three';
import { MotionController } from 'three-stdlib';
export declare class XRControllerModel extends Group {
    envMap: Texture | null;
    envMapIntensity: number;
    motionController: MotionController | null;
    scene: Object3D | null;
    constructor();
    setEnvironmentMap(envMap: Texture | null, envMapIntensity?: number): XRControllerModel;
    setEnvironmentMapIntensity(envMapIntensity: number): XRControllerModel;
    connectModel(scene: Object3D): void;
    connectMotionController(motionController: MotionController): void;
    /**
     * Polls data from the XRInputSource and updates the model's components to match
     * the real world data
     */
    updateMatrixWorld(force: boolean): void;
    disconnect(): void;
    dispose(): void;
}
