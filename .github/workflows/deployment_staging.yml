# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting (Staging Environment)

on:
  push:
    branches: ["release/*"]

defaults:
  run:
    working-directory: ./my-webxr-app

jobs:
  build_test_deploy_review:
    name: Build - Test - Deploy Preview
    uses: ./.github/workflows/deployment_dev.yml

  build_and_deploy_to_staging:
    name: Staging
    needs: build_test_deploy_review
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          touch .env
          echo VITE_ROLLBAR_ACCESS_TOKEN=${{ secrets.ROLLBAR_ACCESS_TOKEN }} >> .env
          echo VITE_ROLLBAR_ENVIRONMENT=${{ secrets.ROLLBAR_DEV_ENVIRONMENT }} >> .env
          echo VITE_IS_TESTING=true >> .env
          cat .env
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          entrypoint: ./my-webxr-app
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_OCULUS_3D_RENDER_STAGING }}'
          channelId: live
          projectId: oculus-3d-render-staging
